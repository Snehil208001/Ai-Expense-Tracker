[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
# Railway defaults to PORT=8080; we use 3000. Set PORT=3000 in Variables if 502 persists.
